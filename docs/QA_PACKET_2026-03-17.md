# QA Packet - 2026-03-17

## Scope

Local prototype verification for the current client-only Minecraft builder app.

External target:

- public preview URL: `https://reginaldvw2007-wq.github.io/minecraft-builder-app/`

## Automated Checks

- `npm run test` - pass
- `npm run lint` - pass
- `npm run build` - pass
- `npm run smoke:browser` - pass

## Browser Smoke Evidence

Environment:

- local Vite dev server launched by `scripts/browser_smoke.sh`
- browser automation: Playwright CLI

Observed pass conditions:

1. App loaded with hero, intake, overview, layer, and build-guide sections visible.
2. The viewport was forced to a phone-sized layout before interaction.
3. Two real local image files were uploaded through the shared intake control.
4. Uploaded-photo status text rendered after the intake update.
5. Export JSON button triggered a download of a build-plan artifact.
6. Export confirmation text rendered in the hero panel after the download.
7. Sticky mobile hotbar actions remained visible in the phone viewport screenshot.

Artifacts:

- screenshot: `output/playwright/app-smoke.png`
- server log: `output/playwright/browser-smoke-server.log`

## Residual QA Gaps

- no long-running analysis state exists yet to validate progress behavior
- no invalid-upload smoke coverage exists for rejected files or partial acceptance
- no persistence flow exists to validate across refreshes

## QA Verdict

- verdict: `PASS` for local and static-preview verification
- limit: this is not evidence for geometry accuracy or real-photo reconstruction quality
